info:
  name: delete group
  type: http

http:
  method: DELETE
  url: http://localhost:5000/api/groups/<group_id>
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
