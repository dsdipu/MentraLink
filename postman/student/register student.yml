info:
  name: register student
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:5000/api/auth/register
  body:
    type: json
    data: '{ "name": "Tanha", "email": "tanha224517@gmail.com", "password": "test1234", "role": "STUDENT" }'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
