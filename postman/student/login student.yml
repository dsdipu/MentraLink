info:
  name: login student
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:5000/api/auth/register
  body:
    type: json
    data: |-
      {
        "email": "dipu.37@gmail.com",
        "password":"dipu37"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
